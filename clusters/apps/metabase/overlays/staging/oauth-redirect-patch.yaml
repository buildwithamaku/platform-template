# Per-env OAuth redirect URL (must match the OIDC app's authorized redirect URI).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: oauth2-proxy
spec:
  template:
    spec:
      containers:
        - name: oauth2-proxy
          env:
            - name: OAUTH2_PROXY_REDIRECT_URL
              value: https://bi.staging.amakusolutions.com/oauth2/callback
